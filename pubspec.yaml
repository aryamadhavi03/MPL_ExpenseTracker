name: expense_tracker
description: "A new Flutter project."

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ">=3.7.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  firebase_core: ^2.24.2  # Correct latest version
  firebase_auth: ^4.17.6  # Correct latest version
  cloud_firestore: ^4.15.6  # Added Cloud Firestore
  flutter_fortune_wheel: ^1.3.0
  flutter_local_notifications: ^16.3.0
  timezone: ^0.9.2  

  flutter_localizations:
    sdk: flutter

  provider: ^6.0.5
  flutter_calendar_carousel: ^2.0.2
  fl_chart: ^0.63.0
  cupertino_icons: ^1.0.8
  shared_preferences: ^2.0.18
  google_sign_in: ^6.2.1

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true
